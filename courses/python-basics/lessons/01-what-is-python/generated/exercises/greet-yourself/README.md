# Greet yourself

Write a function `greet(name)` that returns a personalized greeting. For example, if you call `greet("Alice")`, it should return `"Hello, Alice!"`.

Start from `starter.py`. The hidden tests in `test_exercise.py` must pass.
