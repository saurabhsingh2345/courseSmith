# Greet yourself

Write a function `greet(name)` that returns a greeting message. For example, if you call `greet("Ada")`, it should return "Hello, Ada!"

Start from `starter.py`. The hidden tests in `test_exercise.py` must pass.
